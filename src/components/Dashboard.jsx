import React from 'react';
import { Activity, CircleGauge, Radio, Waves } from 'lucide-react';

export default function Dashboard({ flowing, readings, sensorState, filtrationState }) { const metrics = [['pH', readings.pH, ''], ['TURBIDITY', readings.turbidity, 'NTU'], ['TEMP', readings.temperature, '°C'], ['CONDUCTIVITY', readings.conductivity, 'µS'], ['OXYGEN', readings.oxygen, 'mg/L']]; const current = Number(readings.turbidity); const points = [61, 64, 68, 63, current].map((value, index) => `${index * 25},${44 - Math.min(value, 70) * 0.42}`).join(' '); return <section className="dashboard panel"><div className="dashboard-heading"><div><div className="panel-kicker">CONTROL DASHBOARD</div><h2>System telemetry</h2></div><div className="status-orb"><Activity size={15} /></div></div><div className="system-grid"><div><small><Waves size={12} /> WATER FLOW</small><strong className={flowing ? 'green' : 'amber'}>{flowing ? 'ACTIVE' : 'STOPPED'}</strong></div><div><small><Radio size={12} /> SENSOR</small><strong className={sensorState === 'ONLINE' ? 'green' : 'amber'}>{sensorState}</strong></div><div><small><CircleGauge size={12} /> SAMPLE CHAMBER</small><strong className="green">READY</strong></div><div><small><Activity size={12} /> FILTRATION</small><strong className={filtrationState === 'COMPLETE' ? 'green' : filtrationState === 'RUNNING' ? 'amber' : ''}>{filtrationState}</strong></div></div><div className="metrics-grid">{metrics.map(([label, value, unit]) => <div className="metric" key={label}><span>{label}</span><b>{value}<em>{unit}</em></b></div>)}</div><div className="trend-block"><div className="trend-heading"><span>TURBIDITY TREND</span><b>{current} NTU</b></div><svg viewBox="0 0 100 50" preserveAspectRatio="none" role="img" aria-label="Simulated turbidity trend"><path d="M0 44H100" /><polyline points={points} /></svg><div className="trend-axis"><span>START</span><span>SIMULATED LIVE</span><span>NOW</span></div></div><div className="data-caption">SIMULATED DEMO DATA <span>·</span> REFRESHED LIVE</div></section>; }
